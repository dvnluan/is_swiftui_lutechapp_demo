import Foundation
import SwiftUI
import Combine

/// Ứng dụng màu chủ đạo (đỏ mận)
struct AppTheme {
    static let primaryColor = Color(red: 0.55, green: 0.0, blue: 0.15) // Đỏ mận
    static let secondaryColor = Color.gray
    static let backgroundColor = Color(UIColor.systemGroupedBackground)
    static let textColor = Color.black
}
